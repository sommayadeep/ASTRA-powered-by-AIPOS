/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep conservative defaults — disable experimental features that cause
  // dev-server routing instability (e.g., typedRoutes).
};

export default nextConfig;
